services:
  juicy-check:
    build:
      context: .
      dockerfile: Dockerfile
    restart: unless-stopped
    ports:
      - "6067:6067"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
